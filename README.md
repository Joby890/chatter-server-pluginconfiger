# chatter-server-pluginconfiger
A Chatter plugin to allow configuration of plugins in chatter

## Current features
Enable and disable server plugins

## To-Do Features
Allow for config changes dynamically during runtime.
